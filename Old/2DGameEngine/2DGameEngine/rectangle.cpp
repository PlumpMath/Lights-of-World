#include "rectangle.hpp"

namespace engine { using namespace maths; namespace graphics {

	KX_ScreenRect::KX_ScreenRect(float x, float y, float w, float h)
	{
		// Seta o vetor coordinates com os parametros;
		coordinates.x = x;
	}

} }